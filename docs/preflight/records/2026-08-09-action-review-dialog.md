# Component-Sourcing Preflight — ActionReviewDialog + vendored Dialog

- Target app/surface and component job: mission-control-kanban - accessible action-review step (fleet EUX-09) replacing the native confirm()/alert() guards on consequential operator actions (task/agent/session/workspace deletes, settings reset, repeated webhook retry) per the 2026-08-09 fleet UI/UX audit (#139); plus the vendored shadcn dialog it composes
- Target-app component checked: searched src/components and src/components/ui - no dialog primitive exists; the 6 modal overlays (AgentModal, TaskModal, GitHubImportModal, WorkspaceDashboard x2, GitHubIssueDraftPanel) are hand-rolled fixed-inset divs without focus trap or dialog semantics, and 5 confirm() plus 11 alert() call sites guarded consequential actions
- Component Marketplace primitive checked: ActionReviewDialog + action-review-contract exist in component-marketplace src/components/ui at 6a49e75 (Docs/action-review-dialog.md, EUX-09); the marketplace is the fleet source primitive and this record covers its copy-own adaptation into MCK
- External pools checked or deliberately skipped: shadcn/ui Dialog (new-york, vendored per the repo's existing components.json) composed over Radix @radix-ui/react-dialog for the focus trap, Escape, aria-modal, and focus return; Lucide icons already in the repo; no AlertDialog pool component adopted because the fleet contract requires the typed four-question consequence summary shadcn AlertDialog does not carry
- Chosen source lane and why: copy-own from the local Component Marketplace (fleet primitive lane), adapted to MCK conventions - `mc-*` Tailwind palette, plain house buttons instead of the marketplace `Button` (avoids a `class-variance-authority` dependency), no `animate-*` classes because the repo has no `tailwindcss-animate` plugin; `dialog.tsx` vendored via the shadcn lane the repo already declares in `components.json`
- License/access/dependency result: component-marketplace is local iMelki-owned code; shadcn/ui is MIT; one new dependency @radix-ui/react-dialog@1.1.23 (MIT), consistent with the repo's existing @radix-ui/react-slot and @radix-ui/react-tabs; lucide-react and tailwind-merge/clsx already present
- Proof expected before closeout: npm run lint + tsc --noEmit + npm run build green; scripts/verify-component-sourcing-preflight.mjs passes with this record; Test-FrontendComponentSourcingPreflight.ps1 -RequireKnownPoolMention passes; npm run test:action-review pins the contract; grep shows zero native confirm(/alert( guards left in src

Covers: src/components/ui/dialog.tsx, src/components/ui/action-review-dialog.tsx, src/components/ui/action-review-contract.ts
