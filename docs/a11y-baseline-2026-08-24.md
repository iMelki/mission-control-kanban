# Accessibility baseline - 2026-08-24

The first keyboard, focus-visible, contrast and axe measurement this app has ever had.
Before this, the UI/UX scoring programme scored a 30%-weight dimension blind here.

> ## Coverage correction, same day
>
> **The first version of this document reported focus/keyboard numbers measured
> over 4.9% of the app's controls, and said nothing about it.**
>
> The harness enumerated each surface's focusable set, focused the controls one
> at a time, and skipped anything that failed its usability re-check. It had no
> way to tell "this control is legitimately hidden" from "this control no longer
> exists because focusing an earlier one unmounted it" -- an unmounted node does
> not throw, it just reports a 0x0 rectangle. 1600 of the 1816 controls the
> report itself had enumerated left the run through that path, with **zero**
> errors and **zero** failure entries.
>
> | | first version | corrected |
> | --- | --- | --- |
> | Focus/keyboard controls measured | 206 | **4204** |
> | Of a population of | 1816 (itself under-counted) | **4208** |
> | Coverage | **11.3%**, unstated | **99.9%**, stated in the report |
> | Reported when coverage is incomplete | nothing | `status: "partial"`, a per-surface breakdown, and exit `3` |
>
> The 1816 denominator was itself too low: the inventory was taken before the
> surfaces finished mounting, so `/workspace/assistants` was recorded as having
> 24 focusable controls when it settles at 1058. Against the settled population
> the original run covered **4.9%**.
>
> Every focus number below has been re-measured. Numbers that are not focus
> numbers (contrast, axe) were never affected.

> ## Second correction, 2026-08-25
>
> The three follow-ups this document opened have been worked. Two of the
> numbers published above and below do not survive them.
>
> **The "focus unobscured" figure was measuring the probe, not the app.**
> `3234 / 4194` (and the earlier `208/208`) came from hit-testing the centre of
> each control's *full border box*. On this app at 390x844 a 312px card sits
> inside a flex column whose scroll window is 16px tall, so the midpoint is
> below the box that paints it and `elementFromPoint` returns something else.
> 157 of 157 sampled failures passed the same test with the pass's priming Tab
> removed, and 157 of 157 passed after centring the control in every scroll
> ancestor; none had a hidden, off-canvas, or `checkVisibility() === false`
> ancestor. Occlusion is now measured on the control's **visible** rect. #155 is
> closed as a probe artifact, not a UI defect.
>
> **"99.9% coverage, 18 of 18 surfaces at full coverage" is not reproducible.**
> Re-running the *unmodified* harness four times in one session on
> `/workspace/assistants` gave 99.9%, 92.7%, 88.7% and 81.7% at mobile, and the
> surface's own settled population was observed at 1057, 1058, 1062, 1064, 1066,
> 22 and — on `/` at mobile — **0**, which the coverage reporter scored as
> `full` at 100%. That headline is one draw from a distribution. Tracked in
> [#159](https://github.com/iMelki/mission-control-kanban/issues/159); the
> figure is left in place below with this correction attached rather than
> silently restated at a number that is no more stable.
>
> Because of that, "the change did not regress coverage" was tested by
> **alternating** the unmodified harness and this one on the same surface, so
> drift over the session hits both equally:
>
> | round | harness | mobile coverage | desktop coverage | mobile unobscured |
> | --- | --- | --- | --- | --- |
> | 1 | unmodified | 88.7% (942/1062) | 78.3% (833/1064) | 479/941 |
> | 1 | this one | **94.7%** (1008/1064) | 100% (20/20, board never mounted) | **1007/1007** |
> | 2 | unmodified | 81.7% (871/1066) | 85.5% (911/1066) | 444/870 |
> | 2 | this one | **98.1%** (1046/1066) | **99.9%** (1065/1066) | **1044/1045** |
>
> Coverage is equal or better in every paired comparison whose population
> actually mounted, and the occlusion figure moves from about half to all of it.
> The one control at mobile that still reads obscured in round 2 matters: the
> detector has not been turned into something that always passes.
>
> **Roving `tabindex="-1"` children are now audited, but the reported focus-ring
> failures were false negatives.** An arrow-key pass reaches every one
> (`rovingEnumeratedNotAudited: 0`), which closes #154's reachability gap. The
> detector then rejected each real ring because it judged the whole computed
> `box-shadow` list by its one transparent Tailwind bookkeeping component. See
> the third correction below.

> ## Third correction, 2026-08-25
>
> **The cockpit tab result was a detector defect, not a missing-ring UI defect.**
> The probe treated computed `box-shadow` as one value and rejected the whole
> comma-separated list if any component was transparent. Tailwind's ring
> plumbing appends a transparent `--tw-shadow` component to every painted ring,
> so the old test made every Tailwind ring unverifiable by construction.
>
> Source history independently confirms that `src/components/ui/tabs.tsx` has
> carried `focus-visible:ring-2 focus-visible:ring-mc-accent/60` since
> `de22f9aaa39c59464b7868d4494cd42170986217` on 2026-08-02. The ten
> Tab-reachable selected `Board` controls and all forty arrow-only tab controls
> therefore had a visible ring during the baseline runs. Rescoring the same
> evidence leaves **8 true missing indicators**, all four `/settings` inputs at
> both viewports, rather than 18 Tab-reachable plus 40 roving failures.
>
> The old `/60` ring composites to **3.4075:1** over `#0d1117`: above WCAG
> 2.4.11's 3:1 non-text minimum, but below this repository's chosen 4.5:1 focus
> floor. The UI change lifts it to full `#58a6ff`, **7.4918:1**. That is internal
> floor hardening, not remediation of an absent cockpit indicator. The detector
> now scores each shadow component separately and requires both an opaque colour
> and geometry capable of painting a band; authored outer and inset
> `0 0 0 -2px` controls ensure colour alone cannot pass.

## 2026-08-25 correction proof status

The full nine-route BEFORE receipt is
`artifacts/surface-a11y-before/a11y-report.json` with caller output in
`artifacts/before-sweep.log`: 1,767 focus checks, 1,749 reported visible,
40/40 roving controls reached, 0 reported visible under the broken parser, and
55.3% focus coverage in this particular #159-unstable draw. Its 18 named focus
failures reconcile to 8 settings inputs and 10 selected-tab false negatives.

The UI source is content-bound to the preserved port 5391 dev server. Its
compiled workspace hot update has SHA-256
`EC5532067B15DF3A110B6D908D30477DD5866A9E15A60E743E32E0116B1081CC`, and
the compiled workspace chunk has SHA-256
`F1A560C9D5D4AC33B9FC29A43BEEC419B3BDD4FB9A452F101C99A79217461FBC`;
both contain full `focus-visible:ring-mc-accent` and neither contains the old
`focus-visible:ring-mc-accent/60` class.

Application-level AFTER proof is **deferred, not passing**. The exact caller
against that server exited `2` after `/workspace/assistants` rendered zero
authored controls and `nextjs-portal` obscured the injected controls. The server
then reported `Jest worker encountered 2 child process exceptions, exceeding
retry limit`; a bounded readback still returned HTTP 500 after memory fell to
69.7%, below the 78% pressure gate. PID 41212 is intentionally preserved and no
replacement server was started. Receipts:

- `artifacts/after-e918adc-sweep.log`
- `artifacts/surface-a11y-after-e918adc/self-proof.json`
- `artifacts/dev-server.log`
- `artifacts/a11y-after-deferral-2026-08-25.json`

The five cockpit clipping records also remain deliberately stale until the same
source-bound UI is healthy enough for the canonical clipping probe. Do not read
the compiled-asset binding as a substitute for the missing AFTER sweep or the
missing clipping capture. `npm run surfaces:check` exits `1`, naming exactly
those five routes, the digest move `eb3af727177054dd -> 57c47aa411d855f3`, and
`src/components/ui/tabs.tsx` as the one changed rendering file; caller output is
retained in `artifacts/surfaces-check-stale-before-refresh.log`.

## What was measured

| | |
| --- | --- |
| App source measured | `edcc93aaeaa1a4b3c0b58b5ef5cc87f30f00c615` (`src/` clean at that sha) |
| Surfaces | all 9 that `docs/captured-surfaces.json` marks `capture: "required"` |
| Viewports | mobile 390x844, desktop 1440x900 (both from the manifest) |
| Harness | `scripts/probe-surface-a11y.mjs` |
| axe-core | 4.11.1, tags `wcag2a wcag2aa wcag21a wcag21aa wcag22aa` |

The route list is read from the manifest, so the probe cannot drift from the
captured-surfaces gate. Every one of the 9 required surfaces now has a11y
evidence; none was skipped or deferred.

## Headline numbers

| Metric | Result |
| --- | --- |
| **Focus/keyboard coverage** | **4204 / 4208 Tab-reachable controls (99.9%)** |
| Surfaces at full coverage | 18 of 18 **in this run only** -- today's reproduction of the same sweep on the same code gave 17 of 18 and exited `3`. See "Second correction" and [#159](https://github.com/iMelki/mission-control-kanban/issues/159). |
| Samples destroyed while measuring | 0 |
| Roving `tabindex="-1"` children enumerated but not focus-audited | **0** -- an arrow-key pass now measures them ([#154](https://github.com/iMelki/mission-control-kanban/issues/154)) |
| Roving children reached by ArrowRight with a visible focus indicator | ~~**0 of 4 per cockpit, both viewports**~~ **withdrawn** -- all 40 carried a visible `/60` ring; the whole-list shadow parser rejected it |
| Keyboard tab stops walked | **635** |
| Keyboard traps | **0** |
| Focus checks | **4194** |
| Focus indicator visible | ~~**4176 / 4194**~~ **4186 / 4194 when the same observations are rescored** -- 10 selected-tab false negatives removed; 8 `/settings` inputs remain |
| Focus unobscured | ~~3234 / 4194~~ **withdrawn** -- measured the probe's scroll state, see "Second correction" and [#155](https://github.com/iMelki/mission-control-kanban/issues/155). Re-measured on the visible rect, every cockpit reads 100% of measured controls unobscured at BOTH viewports |
| Contrast pairs measured | **527** |
| Contrast pairs passing AA | **508 / 527** |
| Minimum contrast ratio | **2.87 : 1** |
| axe violations | **39** |
| axe blocking-incomplete (critical/serious) | **4** |

The report writes `status` and `coverage` as its first two keys, so no focus
number in it can be read without the coverage it was measured at. A sweep with
any partial surface exits `3`.

Per-surface detail is in `artifacts/surface-a11y/a11y-report.json` (gitignored;
regenerate with the command below).

Desktop viewport shown; both are recorded in the JSON. "coverage" is measured
controls over the settled Tab-reachable population of that surface.

| Surface | coverage | axe | contrast (min) | focus visible | unobscured |
| --- | --- | --- | --- | --- | --- |
| `/` | 26/26 (100%) | 0 | 19/19 (4.95) | 26/26 | 26/26 |
| `/n8n-sync-history` | 4/4 (100%) | 1 | 14/15 (4.04) | 4/4 | 4/4 |
| `/runtime-regression` | 1/1 (100%) | 0 | 15/15 (5.32) | 1/1 | 1/1 |
| `/settings` | 31/31 (100%) | 0 (+1 bi) | 26/26 (4.95) | 27/31 | 31/31 |
| `/workspace/assistants` | 1057/1058 (99.9%) | 3 | 32/34 (3.43) | ~~1055/1056~~ 1056/1056 | 1056/1056 |
| `/workspace/memsys` | 369/370 (99.7%) | 4 | 32/34 (3.43) | ~~367/368~~ 368/368 | 368/368 |
| `/workspace/content-factory` | 68/68 (100%) | 4 (+1 bi) | 46/49 (2.87) | ~~66/67~~ 67/67 | 67/67 |
| `/workspace/asimtop` | 60/60 (100%) | 4 | 32/34 (3.43) | ~~58/59~~ 59/59 | 59/59 |
| `/workspace/frontend-revenue` | 486/486 (100%) | 4 | 39/42 (2.87) | ~~484/485~~ 485/485 | 485/485 |

The four workspace cockpits were previously recorded as having 24-71 focusable
controls each. They settle at 60-1058. The earlier figures were inventories
taken roughly 700ms after load, before the surfaces had finished mounting;
the population is stable from about two seconds and does not grow further under
scrolling (measured at both viewports).

**~~New finding, only visible at this sample size:~~ RESOLVED as a probe
artifact.** On MOBILE, 960 of the 4194 measured controls were reported outside
the viewport when focused while the same surfaces read 100% unobscured on
desktop. Filed as
[#155](https://github.com/iMelki/mission-control-kanban/issues/155) for triage
rather than asserted as a defect here -- correctly, because it was not one. It
was the occlusion test hit-testing the geometric midpoint of controls that are
taller than the scroll window clipping them. Re-measured on the visible rect,
the same surfaces at mobile read:

| surface (mobile) | unobscured, midpoint test | unobscured, visible-rect test |
| --- | --- | --- |
| `/workspace/assistants` | 539/1056 | 833/833 |
| `/workspace/frontend-revenue` | 254/485 | 485/485 |
| `/workspace/memsys` | 195/368 | 366/366 |
| `/workspace/content-factory` | 44/65 | 64/64 |
| `/workspace/asimtop` | 41/59 | 58/58 |

(Denominators differ because coverage varies between runs -- see #159. Every
measured control on every cockpit came back unobscured.)

## The probe proves itself before it reports

A scan that reports zero violations is worthless unless it can be shown capable
of reporting one. Every run injects a known-bad element for each detector,
requires the detector to name it, removes it, and requires the detector to stop
naming it. All 18 legs must pass or the run exits `2` and writes no report.

**Pre-recovery history:** two of those 18 were flaky and failed closed because
they compared whole-page axe node counts, which this app moves between two scans
of the same page. Roughly half of those runs were refused with `PROBE IS DEAD`
after ~90s having measured nothing. This was reproduced on the unmodified
harness (1 of 3 consecutive `--self-proof` runs dead; a full sweep needed 4
attempts). It was tracked in
[#157](https://github.com/iMelki/mission-control-kanban/issues/157) rather than
worked around. The 2026-08-26 recovery scopes those two legs to the harness's
own injected selectors, which is what the other legs already do.

The self-proof used to run on `/` -- the one required surface whose focusable
population is small and static, i.e. the single route where the sample cannot
collapse. **A self-test that cannot fail on the thing it guards is worthless.**
It now runs on the first nested required surface, and its last leg fails if the
chosen route turns out to be structurally incapable of collapsing.

The block below is the 18-leg success contract. The 2026-08-26 post-recovery
caller observed all 18 legs passing after the final deliberate negative
mutation was restored; the earlier refused artifacts remain historical evidence.

```
PASS contrast detector flags the injected low-contrast text
       [measured 1.92:1 (required 4.5) -> pass=false]
PASS the injected run is present BY NAME in the failing set
       [failed 1 -> 3; injected run in failing list=true]
PASS axe NAMES each injected node under its own rule (not just a new rule id)
       [color-contrast nodes 1 -> 4 names #__sp_contrast=true;
        image-alt nodes 0 -> 1 names #__sp_img=true]
PASS focus detector sees the suppressed indicator
       [active=true outline=false shadow=false]
PASS a TRANSPARENT 2px outline is NOT scored as a visible focus ring
       [visible=false transparentOutlineOnly=true outlineColor=rgba(0, 0, 0, 0)]
PASS opaque outer and inset shadows contracted behind the control are NOT
     scored as visible
       [outerVisible=false insetVisible=false; both computed shadows retain
        rgb(0, 0, 255) 0px 0px 0px -2px]
PASS the injected run is GONE BY NAME after removal
       [injected run still present=false]
PASS axe returns to baseline after removal (per-rule node counts, by name)
       [color-contrast nodes 4 -> 1 (baseline 1); image-alt nodes 1 -> 0 (baseline 0)]
PASS the injected controls are measured while present and absent after removal
       [before 0 -> present 4/4 -> after removal 0/0]
PASS COVERAGE: a self-destroying sample is reported as PARTIAL and named
       [status=partial coverage=7.7% measured=1/13 stale=12
        destroyedBy="sample destroyer" unaccounted=0]
PASS COVERAGE: an identical STABLE sample is reported as FULL at 100%
       [status=full coverage=100% measured=13/13 unaccounted=0]
PASS ROOT CAUSE: a tabindex="-1" roving control is not focused, sample survives
       [status=full measured=12/12 notTabReachable=1 sampleDestroyed=false]
PASS the self-proof route is CAPABLE of exhibiting sample collapse
       [route=/workspace/assistants population 20 -> 19
        autoActivationTabs=5 rovingFocusables=5]
PASS OCCLUSION: a control under an opaque overlay is reported OBSCURED, a clear
     one is not
       [clear unobscured=true; covered unobscured=false
        obscuredBy="div#__a11y_occ_veil"; named in failures=true]
PASS OCCLUSION: the same control reads unobscured once the overlay is removed
       [covered-after-unveil unobscured=true]
PASS OCCLUSION: a clipped control is scored on its VISIBLE area, not its midpoint
       [clipped unobscured=true visibleFraction=0.1
        clippedBy="div#__a11y_occ_clipbox [hidden/hidden]"]
PASS ROVING: both arrow-only children measured THROUGH a panel unmount,
     suppressed ring named
       [measured=2/2 notAudited=0 visible=1 failures=1
        namesChildOne=true namesChildTwo=false]
PASS ROVING: an identical widget with intact rings reports 2/2 measured and
     ZERO failures
       [measured=2/2 visible=2 failures=0]
```

Adding the two contracted-shadow controls made the first exact-caller run exit
`2` for the right reason: the scoped accounting leg still required the old
two-control fixture while reporting `before 0 -> present 4/4 -> after removal
0/0`. The contract now requires all four authored focus controls to be present
and measured. That refused run is retained at
`artifacts/selfproof-current-detector-3021.log`; it is not presented as a green
receipt.

The five occlusion and roving legs added on 2026-08-25 close the gap that let
issue [#155](https://github.com/iMelki/mission-control-kanban/issues/155) be published
twice. **The occlusion detector had never been shown able to report a covered
control** -- twelve legs, not one of them touching `unobscured`, behind the
published `208/208` and `3234/4194`. It now proves itself in three directions
every run: covered is reported and named, clean is reported clean once the
overlay is removed, and a control clipped to a tenth of its box is NOT scored
obscured. That last one is #155's exact shape, authored, so the midpoint test
cannot come back unnoticed.

The two ROVING legs carry a real roving widget -- one Tab-reachable entry, two
`tabindex="-1"` children, arrow keys that move both focus and the `tabindex="0"`
the way Radix does -- whose first child unmounts a panel on arrival. The pass is
therefore proven to walk *through* the exact event that made #154 a trade-off.

Three of those legs carry the harness's own authored control content -- a focus
sample that provably destroys itself, an identical sample that does not, and a
roving `tabindex="-1"` destroyer -- so the coverage reporter is proven in both
directions on every run rather than depending on whichever app surface happens
to misbehave that day.

Two legs were replaced because they compared **quantities that the app moves on
its own**, which is a false-alarm mode that fails closed and blocks legitimate
runs:

- *"axe reports at least one NEW violation id"* compares rule-ID **sets**. On
  any surface that already reports `color-contrast`, the injected low-contrast
  text adds no new id and that half of the leg silently stops testing anything.
  Measured on `/workspace/frontend-revenue`: `violations 4 -> 5; new ids:
  image-alt`. On a surface that also already has an alt-less image the leg goes
  fully dead -- reproduced on an authored control page carrying both rules:
  `violations 4 -> 4; new ids: (none)`, `PROBE IS DEAD`, exit 2, no report
  written. It now asserts that axe **names the injected selector inside that
  rule's own targets**, plus per-rule node counts. The sibling mission-control
  harness hit the same trap and solved it the same way
  (`tests/e2e/accessibility-contract.spec.ts`, controls A and B); that approach
  is reused here rather than a third one invented.
- *"focusable population returns to baseline after removal"* compared a
  whole-page focus count. Once the self-proof moved off the static root it read
  `baseline 7 -> restored 9` and killed a healthy run. It is now scoped to the
  harness's own injected container, where the expected values are exactly 4 and
  0. The two contrast legs got the same treatment for the same reason
  (`failed 3 -> 2 (baseline 1)` on a surface whose own content changed
  mid-proof).

This is not ceremony. The self-proof caught real defects **in the harness
itself** before any number was published:

1. **A silently emptied focusable set.** A swallowed exception in the focus pass
   made every element "unusable" and the pass reported `checked: 0` — a clean
   zero produced by measuring nothing. The self-proof compared baseline against
   post-removal and refused the run. The catch now surfaces the error.
2. **A transparent outline scored as a visible focus ring.** See below.

What it did **not** catch, because it was structurally unable to, is the
coverage collapse -- it ran on the only route where the collapse does not
happen. That is fixed above, and is the reason the route-capability leg exists.

## Three corrections made before publishing these numbers

An earlier run of this harness produced different, wrong numbers. They are
recorded here because the corrections are the substance of the result.

Every figure in this section is quoted at the **4.9% coverage** of that run.
The corrections themselves still hold -- they are about how a control is
scored, not how many were scored -- but the counts are a sample of about one
control in twenty, not totals.

**1. Next.js dev-overlay contamination.** The first run attributed **16 of 16**
focus-obscured failures to a single control: the "Open Next.js Dev Tools"
button injected by the dev server. It does not exist in a production build.
Every detector now excludes dev chrome, and the app's real result is
**208/208 unobscured** — the app had no obscured-focus problem at all.

**2. A fabricated keyboard trap.** The first run reported a keyboard trap on
`/settings` at both viewports. It was a defect in the probe: trap detection
keyed on `tag|class|label|size`, and `src/app/settings/page.tsx` has four
adjacent inputs (lines 149, 166, 183, 213) that share an identical className,
identical size and an empty label. Four different controls produced one
identical key, which read as "Tab never leaves this element". Identity is now
a per-element stamp, and the corrected result is **0 traps**.

**3. A transparent outline counted as a focus ring.** Tailwind 3.4.19 compiles
`outline-none` to `outline: 2px solid transparent` (confirmed in this repo's
generated CSS). The focus check — ported from component-marketplace — tested
only `outlineStyle !== 'none' && outlineWidth > 0`, so an **invisible** outline
passed. Correcting it to require a non-transparent outline colour moved the
focus result from a reassuring 208/208 to **150/208**, surfacing 58 reported
failures. That correction was necessary but not sufficient: the third
correction above establishes that its whole-list shadow test could also reject
a real Tailwind ring, so the 58 must not be called 58 UI defects. The transparent
outline flaw exists in the upstream harness too and is worth checking in any
sibling repo that uses Tailwind `outline-none`.

## Findings

| Issue | Finding |
| --- | --- |
| [#150](https://github.com/iMelki/mission-control-kanban/issues/150) | Focus indicators. **The "58 of 208" headline was measured over 4.9% of the app and does not scale.** A later report's 18 Tab-reachable failures also included 10 false negatives from the whole-list `box-shadow` parser. The corrected observed defect is **8**: the four `/settings` inputs sharing one className, at both viewports. All 40 arrow-only cockpit tabs and the 10 selected `Board` tabs already carried a visible `/60` ring; the UI now raises that ring from 3.4075:1 to 7.4918:1 to meet the repository's 4.5:1 floor. Modal and dialog call sites remain **unmeasured, not clean** -- the probe does not open them. |
| [#151](https://github.com/iMelki/mission-control-kanban/issues/151) | 3 distinct colour pairs below AA; worst 2.87:1 on 10px timestamps using `text-mc-text-secondary/60`. Unaffected by the coverage defect -- contrast was never sampled through the focus pass. |
| [#152](https://github.com/iMelki/mission-control-kanban/issues/152) | axe violations: `nested-interactive`, `color-contrast`, `scrollable-region-focusable`, `link-name`, and a **critical** `aria-valid-attr-value`. Also unaffected: axe scans the document, not the focus sample. |
| [#153](https://github.com/iMelki/mission-control-kanban/issues/153) | Bind a11y evidence to the `sourceDigest` staleness gate; add the on-demand npm scripts. |

Follow-ups from the coverage correction, all three closed on 2026-08-25:
[#154](https://github.com/iMelki/mission-control-kanban/issues/154) roving
`tabindex="-1"` children -- **implemented**, an arrow-key pass audits them and
reaches all 40; the original claim that all 40 lacked a visible indicator is
withdrawn as a detector false negative;
[#155](https://github.com/iMelki/mission-control-kanban/issues/155) mobile
off-viewport focus -- **not a UI defect**, a probe artifact, occlusion now
measured on the visible rect;
[#156](https://github.com/iMelki/mission-control-kanban/issues/156) restate
issue [#150](https://github.com/iMelki/mission-control-kanban/issues/150) --
**needs correction again**, because its 18-of-4194 restatement included
10 selected-tab false negatives and described the 40 roving false negatives as
additional defects.

Opened by that work:
[#157](https://github.com/iMelki/mission-control-kanban/issues/157) two
self-proof legs failed closed about half the time; the 2026-08-26 recovery
scopes their counts to the two authored selectors and proves the change in both
directions. Still open until that feature PR lands. Also open:
[#159](https://github.com/iMelki/mission-control-kanban/issues/159) the coverage
reporter scores a surface that rendered zero controls as full coverage, and
ignores its own `unaccounted` invariant.

The original baseline pass fixed no UI source. The 2026-08-25 correction does
change the cockpit ring opacity, so its five shared cockpit captures must be
re-probed and re-recorded before this follow-up can land. Any `src/` edit moves
`sourceDigest` and
invalidates existing clipping captures — proven with a control: adding one
`aria-label` to `src/components/Header.tsx` produced
`captured-surface drift: 6 problem(s) across 9 derived route(s)`, and was
reverted so the gate returned to green. Even the one-attribute `link-name` fix
therefore requires re-probing and re-recording `capturedAt` for 6 surfaces,
which is a coordinated change rather than a drive-by edit.

## Staleness: this rides `sourceDigest`, and is not yet enforced

The deliberate decision (detail in #153) is that a11y evidence should ride the
**existing** `capturedAt.sourceDigest` mechanism rather than growing a second
one — that digest already covers the route's render closure plus
`tailwind.config.ts` and `postcss.config.mjs`, which is exactly what determines
focus rings and contrast.

That wiring is **not done**. Today this document is a point-in-time baseline,
not an enforced gate: nothing forces a re-run after a UI change. Known blind
spots it will inherit — dependency bumps (a Tailwind bump is precisely what
drives the focus finding) and data-dependent rendering across the five cockpits
that share one `sourceDigest` — are recorded in #153 rather than papered over.

## Placement

On-demand, not pre-push. The harness needs a running server, so a hook would
have to skip when none is up, and a skip is fail-open — it would report success
having measured nothing. The compensating control is the self-proof above: the
harness refuses to emit a report it has not earned. That is a control on probe
*validity*, not on *cadence*; cadence is #153.

## Reproduce

```
node scripts/probe-surface-a11y.mjs --self-proof   # 18/18 legs, ~2 min
node scripts/probe-surface-a11y.mjs                # full sweep, ~25 min

# one surface while iterating; the report and banner both record that the run
# was FILTERED, so it cannot be quoted as an app-wide baseline
MCK_A11Y_ROUTES=/workspace/asimtop node scripts/probe-surface-a11y.mjs out/
```

The #157 correction removes the known whole-page-count flake. Its final
2026-08-26 restored run passed all 18 legs once, and the exhaustive caller then
passed the same 18/18 startup proof before completing all 18 surface rows. A
future `DEAD` leg is a failure to investigate, not an instruction to retry until
green.

Exit codes: `0` every surface at full coverage, `2` the self-proof failed and no
report was written, `3` the sweep ran but at least one surface was measured at
partial coverage -- the report is written and says so, and the exit code exists
so a partial sweep cannot be scripted as a complete one.

Knobs: `MCK_A11Y_SELFPROOF_ROUTE`, `MCK_A11Y_ROUTES` (comma-separated route
filter; recorded in the report), `MCK_A11Y_MAX_FOCUS` (0 = unbounded, the
default), `MCK_A11Y_FOCUS_BUDGET_MS` (per-surface wall clock, default 120000;
set `0` for no bound -- a genuinely exhaustive sweep of every surface is roughly
35 minutes because the cockpits carry ~1000 controls each and the pass measures
about five per second), `MCK_A11Y_FOCUS_ROUNDS`, `MCK_A11Y_MAX_TABS`.

Defaults to `MCK_BASE_URL` or `http://127.0.0.1:3021`. Read-only against the
app: it navigates, measures, and removes everything it injects. Output lands in
`artifacts/` (gitignored).

## Provenance

Adapted from component-marketplace `scripts/operator-primitives-visual-check.mjs`
(blob `ca4bcbf5028760ffb61b058331715740de7ff232`). Ported with the algorithm
intact: the focus/Tab/focus `:focus-visible` priming, the three-ancestor
outline/box-shadow walk, the `elementFromPoint` occlusion test, the WCAG tag
set, and the rule that critical/serious *incomplete* findings block rather than
pass silently.

Not ported: that harness selects contrast pairs by hand-authored
`data-contrast-background`/`data-contrast-foreground` attributes. This repo has
**zero** such attributes, so porting the selector verbatim would have measured 0
pairs and reported `minimumRatio: null` — a vacuous zero. Contrast here is
computed from computed style over rendered text, compositing ancestor
backgrounds until opaque. Its keyboard pass is also bound to that app's specific
widgets, which do not exist here, so the keyboard walk is a generic
reachability/trap/indicator traversal over each surface's real focusable set.
